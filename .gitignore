# Secrets & local state — never commit credentials, tokens, or exports.
config.json
token
*.zip

# Build output.
/bin/
/dist/
/loseit-cli
/loseit-cli.exe
*.exe

# Go test/coverage artifacts.
*.out
coverage.txt

# Local-only notes — personal context, kept on disk, never published.
# (CLAUDE.md is intentionally COMMITTED: it documents auth/self-heal for the next
# agent, including headless/cloud runs where a local-only file wouldn't exist.)
HANDOFF.md

# Editor/OS noise.
.DS_Store
.idea/
.vscode/
__pycache__/
